{
  "name": "marshall-cvx-locker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ marshall-cvx-locker",
    "create-local": "graph create --node http://localhost:8020/ marshall-cvx-locker",
    "remove-local": "graph remove --node http://localhost:8020/ marshall-cvx-locker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 marshall-cvx-locker"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
